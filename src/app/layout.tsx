import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "WhatsApp AI Agent — Dashboard",
  description:
    "Manage your WhatsApp AI agent conversations. View messages, switch between AI and human modes, and send manual replies.",
  keywords: ["whatsapp", "ai", "agent", "chatbot", "dashboard"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
